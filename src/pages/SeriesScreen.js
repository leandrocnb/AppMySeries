import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const SeriesPage = ({series}) => (
    <View>
        <Text>Essa é a nova tela.</Text>
    </View>
)

const styles = StyleSheet.create({

});

export default SeriesPage;